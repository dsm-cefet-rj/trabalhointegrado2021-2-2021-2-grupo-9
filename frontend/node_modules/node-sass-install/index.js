#!/usr/bin/env node
const spawn = require('cross-spawn');
spawn.sync('npm', ['install', '--save-dev', 'dart-sass@latest'], { stdio: 'inherit' });
spawn.sync('npm', ['install', '--save-dev', 'node-sass@npm:dart-sass'], { stdio: 'inherit' });

